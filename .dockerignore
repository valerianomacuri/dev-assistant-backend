# Dependencias y artefactos que se reconstruyen dentro de la imagen.
node_modules
dist

# Secretos y entorno local (NUNCA deben entrar a la imagen).
.env
.env.*
!.env.template

# Git / CI.
.git
.gitignore
.github

# Local dev / docs que no hacen falta en runtime.
docker
docs
*.md
!README.md

# Misc.
npm-debug.log*
pnpm-debug.log*
.DS_Store
coverage
.vscode
.idea
